/**
 * Opcode'ы протокола MAX/OneMe — не задокументированы официально, значения
 * взяты из открытых разборов (гайд PronikFire/Max-API-Guide, гист
 * koval01/analysis.md, maxcalls/docs/api/oneme.md). Список даём только по
 * тем командам, что нужны для логина (Фаза 1) — остальные (отправка
 * сообщений, тайпинг и т.д.) добавим в Фазе 2, когда понадобятся воркеру.
 */
export const OPCODE = {
  /** Инициализация сессии устройства — первый запрос на новом соединении. */
  SESSION_INIT: 6,
  /** Запрос SMS-кода по номеру телефона. */
  AUTH_REQUEST: 17,
  /** Подтверждение кода из SMS. */
  AUTH: 18,
  /** Завершение входа токеном, полученным после AUTH. */
  LOGIN: 19,
} as const;
