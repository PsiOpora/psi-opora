apiVersion: apps/v1
kind: Deployment
metadata:
  name: max-bot
  namespace: psi-opora
  labels:
    app: max-bot
spec:
  replicas: 1
  # Новый под поднимается и проходит readinessProbe до того, как убьём
  # старый (maxSurge: 1) — трафик не прерывается на время выката.
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: max-bot
  template:
    metadata:
      labels:
        app: max-bot
    spec:
      # Даём Traefik время убрать под из endpoints до отправки SIGTERM —
      # без этого часть запросов при выкате улетает в уже завершающийся под.
      terminationGracePeriodSeconds: 20
      imagePullSecrets:
        - name: regcred
      containers:
        - name: max-bot
          image: registry.orixon.ru/psi-opora-max-bot:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: psi-opora-env
            - configMapRef:
                name: redis-connection
          env:
            - name: REDIS_URL
              value: ""
            - name: LOG_LEVEL
              value: "error"
          readinessProbe:
            httpGet:
              path: /api/webhook
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /api/webhook
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 15
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 5"]
---
apiVersion: v1
kind: Service
metadata:
  name: max-bot
  namespace: psi-opora
spec:
  type: ClusterIP
  selector:
    app: max-bot
  ports:
    - port: 3000
      targetPort: 3000
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: max-bot-http
  namespace: psi-opora
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`psi-opora-max.orixon.ru`)
      kind: Rule
      services:
        - name: max-bot
          port: 3000
      middlewares:
        - name: redirect-to-https
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: max-bot-https
  namespace: psi-opora
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`psi-opora-max.orixon.ru`)
      kind: Rule
      services:
        - name: max-bot
          port: 3000
  tls:
    certResolver: letsencrypt
