{
  "name": "psi-opora",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ],
  "scripts": {
    "dev:tg": "turbo dev --filter=@psi-opora/tg-bot",
    "dev:max": "turbo dev --filter=@psi-opora/max-bot",
    "dev:dashboard": "turbo dev --filter=@psi-opora/dashboard",
    "dev:landing": "turbo dev --filter=@psi-opora/landing",
    "build": "bun --env-file=.env turbo run build",
    "postinstall": "bun --env-file=.env turbo run build",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/node": "^26.1.0",
    "@types/pg": "^8.20.0",
    "pg": "^8.16.3",
    "turbo": "^2.10.4",
    "typescript": "^6.0.3"
  }
}
