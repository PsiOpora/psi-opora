**/node_modules
**/.next
**/dist
**/.turbo
**/.vercel
**/*.tsbuildinfo
# .git нужен в контексте сборки: build-скрипты пакетов (packages/*) вызывают
# `git clean -xdf .cache dist` перед сборкой (см. packages/api/package.json и др.)
.github
.vscode
.claude
external
bot.log
*.log
